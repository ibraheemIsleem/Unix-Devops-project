version: '3.8' 

services:
  www:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./website:/var/www/html
    ports:
      - "8000:80"
    depends_on:
      - db

  db:
    image: mysql
    restart: always
    volumes:
      - mysql:/var/lib/mysql
      - ./recommendations.sql:/docker-entrypoint-initdb.d/recommendations.sql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD} 
      MYSQL_DATABASE: recommendation_db

  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - 8081:80
    depends_on:
      - db
    environment:
      - PMA_ARBITRARY=1

volumes:
  mysql: